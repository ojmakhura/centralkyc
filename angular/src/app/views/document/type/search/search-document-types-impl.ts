// Generated by andromda-angular cartridge (view\view.component.imp.ts.vsl) CAN EDIT!
import { Component, linkedSignal, Signal } from '@angular/core';
import { SearchDocumentTypesComponent } from '@views/document/type/search/search-document-types';
import { SearchDocumentTypesVarsForm } from '@views/document/type/search/search-document-types';
import { MatRadioChange } from '@angular/material/radio';
import { MatCheckboxChange } from '@angular/material/checkbox';
import { CommonModule } from '@angular/common';
import { FormsModule, ReactiveFormsModule } from '@angular/forms';
import { TranslateModule } from '@ngx-translate/core';
import { MaterialModule } from '@app/material.module';
import { TableComponent } from '@components/table/table';
import { Loader } from "@shared/loader/loader";
import { Field } from '@angular/forms/signals';
import { MatPaginator } from '@angular/material/paginator';
import { Page } from '@app/models/page.model';
import { DocumentTypeDTO } from '@app/models/bw/co/centralkyc/document/type/document-type-dto';

@Component({
  selector: 'app-search-document-types',
  templateUrl: './search-document-types.html',
  styleUrls: ['./search-document-types.scss'],
  standalone: true,
  imports: [
    CommonModule,
    FormsModule,
    ReactiveFormsModule,
    TranslateModule,
    MaterialModule,
    TableComponent,
    Loader,
    Field,
  ],
})
export class SearchDocumentTypesImplComponent extends SearchDocumentTypesComponent {

  override loading = linkedSignal(() => false);
  override loaderMessage = linkedSignal(() => '');

  override documentTypesTableSignal = linkedSignal(() => new Page<DocumentTypeDTO>());

    constructor() {
        super();
    }

    override beforeOnInit(form: SearchDocumentTypesVarsForm): SearchDocumentTypesVarsForm{     
        return form;
    }

    override doNgAfterViewInit(): void {
    this.documentTypesTable?.tablePaginator?.page?.subscribe({
      next: (paginator: MatPaginator) => {
        this.doSearch(paginator.pageIndex, paginator.pageSize);
      },
    });

    this.doSearch();
  }

  doNgOnDestroy(): void { }

  override beforeSearchDocumentTypesSearch(form: any): void {
    this.doSearch();
  }

  private doSearch(pageNumber: number = 0, pageSize: number = 10): void {
    let criteria = this.searchDocumentTypesSignal().criteria;

    this.loading.set(true);
    this.loaderMessage.set(`Loading page ${pageNumber} document types.`);

    this.documentTypeApi.pagedSearch(
      criteria ? criteria : '',
      pageNumber,
      pageSize
    ).subscribe({
      next: (documentTypePage: Page<DocumentTypeDTO>) => {

        this.documentTypesTableSignal.set(documentTypePage);
        this.loading.set(false);
      },
      error: (error) => {
        console.log(error);
        this.toaster.error(
          error.error?.message ? error.error.message : error.message
        );
        this.loading.set(false);
      }
    });
  }
}